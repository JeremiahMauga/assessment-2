from interface import Interface

run_functions = Interface()

# Did not finish return video method (does not udate in customer.csv, only updates in the inventory.csv)
# Unit tests also not fully setup